<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.violinsolo.testlibsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.AdcvActivity"></activity>
        <activity android:name=".activity.TestImageStatusBarActivity" />
        <activity android:name=".activity.TestStatusBarActivity" />
        <activity android:name=".activity.TestGifActivity" />
        <activity
            android:name=".activity.TestPermissionActivity"
            android:label="@string/title_activity_test_permission"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- this is autosize design -->
        <!-- <meta-data -->
        <!-- android:name="design_width_in_dp" -->
        <!-- android:value="360"/> -->
        <!-- <meta-data -->
        <!-- android:name="design_height_in_dp" -->
        <!-- android:value="640"/> -->
        <!-- this is autosize design -->
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>